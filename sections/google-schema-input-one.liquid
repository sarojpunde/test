{{ section.settings.structured_schema }}

{% schema %}
{
    "name": "Google Structured Schema",
    "settings": [
        {
            "type": "textarea",
            "id": "structured_schema",
            "label": "Add Google Schema"
        }
    ]
}
{% endschema %}